[project]
name = "rag"
version = "0.1.0"
description = "A small RAG data processing project."
requires-python = ">=3.10"
dependencies = [
    "langchain-core",
    "langchain-text-splitters",
    "langchain-chroma",
    "langchain-openai",
    "python-dotenv",
]
